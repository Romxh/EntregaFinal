{
  "name": "entregafinalrevisionoficial",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
    "build-css": "node-sass --include-path scss sass/estilos.scss estilos.css --output-style=expanded;
    "watch-css": "nodemon -e sass -x \"npm run build-css\""
  },
  "author": "Romina Martinez",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^7.0.1",
    "nodemon": "^2.0.18"
  }
}
